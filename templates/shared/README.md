# {{ProjectName}}

Project type: {{ProjectType}}
Primary language: {{PrimaryLanguage}}
Framework: {{Framework}}
Package manager: {{PackageManager}}
Deployment target: {{DeploymentTarget}}

AI guidance for this repository lives in `docs/ai/`.

## Common Commands

- Install: `{{InstallCommand}}`
- Test: `{{TestCommand}}`
- Lint: `{{LintCommand}}`
- Build: `{{BuildCommand}}`
- Run: `{{RunCommand}}`

Update these commands after applying the blueprint if the defaults do not match the project.
