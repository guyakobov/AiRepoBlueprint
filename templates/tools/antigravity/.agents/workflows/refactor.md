# Antigravity Workflow: Refactor

Follow the canonical refactor workflow in `docs/ai/workflows.md`.

Preserve behavior and use `{{TestCommand}}` for verification unless a narrower command is available.
